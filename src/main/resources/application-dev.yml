management:
  health:
    mail:
      enabled: false
  server:
    base-path: /actuator

mybatis:
  mapper-locations: classpath:/mybatis-mapper/*Mapper.xml

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      auto-commit: true
      connection-test-query: SELECT 1
      connection-timeout: 10000
      idle-timeout: 30000
      max-lifetime: 900000
      maximum-pool-size: 30
      minimum-idle: 10
      pool-name: HikariCP
      validation-timeout: 1000
    password: root123
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:mysql://localhost:3306/xxl-job-admin?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&serverTimezone=Asia/Shanghai
    username: root
  freemarker:
    charset: UTF-8
    request-context-attribute: request
    settings:
      number_format: 0.##########
    suffix: .ftl
    templateLoaderPath: classpath:/templates/
  mvc:
    servlet:
      load-on-startup: 0
    static-path-pattern: /static/**
  web:
    resources:
      static-locations: classpath:/static/

mailsend:
  cfg:
    service-type: 1
    javamail:
      host: smtp.exmail.qq.com
      port: 465
      user: chandao@middol.com
      pass: Pcn37qK9v4U6!r
      form: 系统信息<chandao@middol.com>
      ssl-enable: true

xxl:
  job:
    accessToken: ''
    i18n: zh_CN
    logretentiondays: 30
    triggerPoolFastMax: 200
    triggerPoolSlowMax: 100
